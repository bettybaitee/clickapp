{% extends 'base.html.twig' %}

{% block title %}Flag{% endblock %}

{% block body %}
    <h1>Flag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ flag.id }}</td>
            </tr>
            <tr>
                <th>Nama</th>
                <td>{{ flag.nama }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_flag_index') }}">back to list</a>

    <a href="{{ path('app_flag_edit', {'id': flag.id}) }}">edit</a>

    {{ include('flag/_delete_form.html.twig') }}
{% endblock %}
