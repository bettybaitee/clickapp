{% extends 'base.html.twig' %}

{% block title %}Permohonan index{% endblock %}

{% block body %}
    <h1>Permohonan index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tajuk</th>
                <th>Sor</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for permohonan in permohonans %}
            <tr>
                <td>{{ permohonan.id }}</td>
                <td>{{ permohonan.tajuk }}</td>
                <td>{{ permohonan.sor }}</td>
                <td>
                    <a href="{{ path('app_permohonan_show', {'id': permohonan.id}) }}">show</a>
                    <a href="{{ path('app_permohonan_edit', {'id': permohonan.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_permohonan_new') }}">Create new</a>
{% endblock %}
