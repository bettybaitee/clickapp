<form method="post" action="{{ path('app_permohonan_delete', {'id': permohonan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ permohonan.id) }}">
    <button class="btn">Delete</button>
</form>
